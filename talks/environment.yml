# Additional tools for the existing woodpecker environment, not a standalone service.
# From talks: make install
# DeckTape is installed with npm afterwards; see README.md.
name: woodpecker
channels:
  - conda-forge
dependencies:
  - quarto =1.9.38
  # Includes npm/npx; use Node 22 LTS for DeckTape and Puppeteer.
  - nodejs >=22,<23
