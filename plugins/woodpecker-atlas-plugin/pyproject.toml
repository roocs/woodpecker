[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "roocs-woodpecker-atlas-plugin"
version = "0.7.1"
description = "External plugin providing ATLAS fixes for Woodpecker"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
dependencies = [
  "roocs-woodpecker>=0.7,<0.8",
  "xarray>=2024.1.0",
]

[project.optional-dependencies]
test = [
  "pytest>=8.0",
]

[project.entry-points."woodpecker.plugins"]
atlas = "woodpecker_atlas_plugin"

[project.urls]
Documentation = "https://roocs.github.io/woodpecker/plugins/"
Issues = "https://github.com/roocs/woodpecker/issues"
Repository = "https://github.com/roocs/woodpecker"

[tool.setuptools.packages.find]
where = ["src"]
include = ["woodpecker_atlas_plugin*"]

[tool.setuptools.package-data]
woodpecker_atlas_plugin = ["recipes/*.json", "recipes/*.yaml", "recipes/*.yml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
